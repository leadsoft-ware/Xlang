/*
This is a test file for Xlang compiler
Writed by xiaokang00010 at Febuary,12th,2021
*/
// for testing
func int test(int:t){
    if(t == 1){return 1;};
    // found one bug on 'int ret = test(t-1) + t;'
    // still cannot fixed in this version
    int ret = test(t-1);
    ret = ret + t;
    return ret;
};

int f = test(10);