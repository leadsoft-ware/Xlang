set_marco(jmp_nofx,{
    mov(reg0,__marco_param0);
    mov(reg1,regpc);
    sub(reg0,reg1);
    jmp(reg0);
})

set_marco(call,{
    push(reg16);
    push(reg17);
    push(reg18);
    jmp_nofx(__marco_param0);
})

set_marco(ret,{
    mov(reg17,0);
    mov(reg15,__marco_param0);
    pop(reg0,8);
    pop(reg1,8);
    pop(reg2,8);
    mov(reg16,reg2);
    mov(reg17,reg1);
    mov(reg18,reg0);
})

main:{
    ret(0);
}

entry:{
    call(main);
}